<div class="col-lg-9">

  <div class="panel panel-flat">
    <div class="panel-heading mt-5">
      <h5 class="panel-title"><%= @auction.name %></h5>
    </div>

    <div class="panel-body">
      <p><%= @auction.description %></p>
    </div>
  </div>

</div>

<div class="col-lg-3">

<%= erb :'partials/_auctiondetail' %>

<% if @user.authenticated? %>
  <%= erb :'partials/_biddermenu' if @auction.active? && @auction.user != @user %>
<% end %>

</div>





  